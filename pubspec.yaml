name: trellis
description: "TRELLIS — the quiet vault. Premium dark personal-finance app (espresso & gold). 5 screens: Home, Cards, Activity, Insights, Settings."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
